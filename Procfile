# Procfile for Railway multi-service deployment
# Use this if deploying API and Worker as separate services from same repo

# API Service - HTTP endpoints
web: npm start

# Worker Service - Background jobs (verification + settlement)
worker: npm run worker
